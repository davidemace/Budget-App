{
  "name": "mace-budget-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "db:migrate:local": "wrangler d1 migrations apply mace-budget-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply mace-budget-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.20.5"
  }
}
